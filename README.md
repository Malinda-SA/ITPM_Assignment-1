ITPM Assignment 1 – Singlish to Sinhala Converter Automation
This project uses Playwright-based automation to verify that the SwiftTranslator website functions correctly and meets expected performance standards.

## Project Setup

## Prerequisites
1. Ensure *Node.js* is installed on your machine.
2. EEnsure you have a working internet connection for website access during testing
### Installation Steps
1. Download or clone the project files to your computer
2. Open the project folder in your terminal or command prompt
3. Install all necessary packages by running:
   ```bash
         npm install0
4.Download the required Playwright browsers using:
      npx playwright install
5.To run all 35 test cases in a visible browser window using Chromium, use this command:
      npx playwright test --project=chromium --headed
      